#include "holberton.h"
/**
* _strcpy - prints string to console, then new line
* @dest: string destination
* @src: string source
* a blank line
* Return: returns 0
*/
char *_strcpy(char *dest, char *src)
{
  dest = src;
  return (dest);
}
