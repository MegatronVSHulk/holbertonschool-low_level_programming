#include "holberton.h"
/**
* _mul - The primary function being carried out
* @a: First input int
* @b: Secodn input int
* a blank line
* Return: returns @val
*/
int mul(int a, int b)
{
int val;
val = (a * b);
return (val);
}
