#include <stdio.h>

/* betty style doc for function main goes there */
int main(void)
{
  char LETTER;
  for (letter = 'a'; letter <= 'z'; letter++){
    if(letter != 'e' && letter != 'q'){
      putchar(letter);
    }
  }
  return (0);
 }
